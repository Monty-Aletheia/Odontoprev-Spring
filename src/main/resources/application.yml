server:
  port: ${SERVER_PORT:8080}

spring:
  application:
    name: challenger
  datasource:
    url: jdbc:oracle:thin:@oracle.fiap.com.br:1521:orcl
    username: rm553873 #rm553912 #rm553912 #rm552626
    password: 110804 #141204 #141204 #080305
    driver-class-name: oracle.jdbc.OracleDriver
    hikari:
      minimum-idle: 1  
      maximum-pool-size: 10 
      idle-timeout: 15000 
      max-lifetime: 600000  
      connection-timeout: 10000  
      validation-timeout: 5000  
      leak-detection-threshold: 5000 
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  devtools:
    restart:
      enabled: true
  flyway:
    enabled: false
    locations: classpath:db/migration
    baseline-on-migrate: true
    connectRetries: 3
    connectTimeout: 30

logging:
  level:
    root: INFO
    br: DEBUG
    org:
      hibernate:
        SQL: DEBUG
        type:
          descriptor:
            sql:
              BasicBinder: TRACE
